class BowlingGame {
  constructor() {
  }

  roll(pin) {
  }

  get_score() {
  }
}

module.exports = BowlingGame;

// Prereq - Do you have npm and node installed?
// At this point you probably should have them installed

// 1 - npm init --yes
// 2 - npm install --save-dev jest
// 3 - Add jest into package.json's scripts.test
// 4 - Try set up the barebone code and run npm test

// JS Jest Setup
// https://jestjs.io/docs/getting-started
